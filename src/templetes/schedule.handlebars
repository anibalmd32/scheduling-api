<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="schedule.css">
  <title>{{title}}</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Courier New', Courier, monospace:
      font-size: 12px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    header {
      margin: 1rem 0;
      text-align: center;
    }

    main {
      width: 100%;
      padding: 1rem;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    tr, td, th {
      border: 1px solid black;
    }

    td, th {
      padding: 0.5rem;
      text-align: center;
    }
  </style>
</head>
<body>
  <header>
    <h1>Horario</h1>
  </header>

  <main>
    <table>
      <thead>
        <tr>
          <th>Hora</th>

          {{#each days}}
            <th>{{this}}</th>
          {{/each}}
        </tr>
      </thead>
      <tbody>
        {{#each hours}}
          <tr>
            <td>{{this}}</td> 
            <td>{{getSubject 'lunes' this}}</td>
            <td>{{getSubject 'martes' this}}</td>
            <td>{{getSubject 'miercoles' this}}</td>
            <td>{{getSubject 'jueves' this}}</td>
            <td>{{getSubject 'viernes' this}}</td>
            <td>{{getSubject 'sábado' this}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </main>
</body>
</html>